# Focus — 2026-04-29T13:24:00

D9 결정 — `## learn 언어 패턴` 의 두번째 표 (역할 분류) 를 **wide-form 폐기, long-form 전환**.

## 적용해야 할 형식

```markdown
| 역할 | 식별 패턴 |
| --- | --------- |
| routes | `config/routes.rb` 도메인 라인·`@RestController` `@RequestMapping`·`*.routes.ts` `router.use` |
| controllers | `*_controller.rb`·`< ApplicationController`·`@RestController` `@Controller`·`*.controller.ts` |
| services | `*_service.rb`·`app/services/**`·`@Service`·`*.service.ts` |
| models | `app/models/**`·`< ApplicationRecord`·`@Entity`·`*.model.ts` `*.entity.ts` |
| helpers | `app/helpers/**`·util/lib 폴더·`*Util.kt` `*Helper.kt`·`*.util.ts` `*.helper.ts` |
| other | 위 어느 것도 아님 |
```

- 2 컬럼 (`역할`, `식별 패턴`).
- 식별 패턴 = 다중 언어·프레임워크 OR 결합 (`·` 구분자).
- 한 행 = 한 역할.
- 사용자 자유 정의 우선, default 는 흔한 패턴 OR 결합으로 첫 사용자 커버.

## 적용 범위 (변경 대상 5 곳)

1. `pilot/skills/learn/SKILL.md` — 역할 분류 default 표 (현재 wide-form `| 역할 | Ruby | Kotlin | TypeScript |`) → 위 long-form 으로 교체. default blockquote 위치 그대로 유지.
2. `workspace/context/config.md` — `## learn 언어 패턴` 의 `### 역할 분류 (wide-form)` 헤딩 → `### 역할 분류` 로 변경 + 본문 표 long-form 으로 교체.
3. `pilot/tests/fixtures/v0.1.0-baseline/config/pass-valid/config.md` — 역할 분류 표를 long-form 으로 교체. PASS 케이스 유지.
4. `pilot/tools/doctor/integrity.py` — `check_workspace_config_sections` 의 역할 분류 표 컬럼 수 검증 로직: `1+N (wide-form)` → `정확히 2 컬럼 (역할, 식별 패턴)`. 헤더 정확 일치 강제.
5. `pilot/tests/tools/test_doctor_integrity.py` — 기존 `error-column-mismatch` fixture 가 wide-form 가정이었으면 long-form 기준으로 갱신. test_pass_valid·test_pass_empty 케이스도 long-form 검증.

## 의존성 추적 표는 변경 없음

`### 의존성 추적` 의 5 언어 wide-form (`| 언어 | 의존성 추출 패턴 |`) 은 그대로 유지. import 문법은 언어별이라 (언어, 패턴) lookup 자연.

## 기존 D1 결정 뒤집힘

D1 의 wide-form 유지는 backward-compat 0 brittle 의도였음. long-form 도 default 가 5 언어/프레임워크 OR 결합으로 모든 기존 패턴 포함하면 동일 보장. 추상화 정확성 우선.

## 사용자 결정 사유 (메인 대화)

> "언어에 대한 제한이 필요할까? 그런 의문이 드는데.."

→ 역할 분류 표의 (역할, 언어) 매트릭스가 사실 (역할, 프레임워크) 의 잘못된 압축임을 발견. Rails/Sinatra/Ktor/Spring/Express/NestJS 가 같은 언어 안에서 갈라져서 언어 차원이 1 급 추상화로 부적합. (역할, 식별 패턴) 단일 차원으로 단순화.

## design doc 갱신 완료 (메인 turn)

- `docs/design-pilot-generic-2026-04-29.md` 의 (a) 본문 + Reviewer Concerns 매트릭스 D9 row 추가.
- `features/01-learn-language-pattern-externalize.md` 의 비즈니스 규칙 표 스키마 (2) 갱신.
- `features/04-doctor-config-validation.md` 의 컬럼 수 강제 룰 갱신.

본 .focus.md 는 후속 generator 가 위 5 곳 변경을 일관되게 적용하도록 가이드.
