# Focus — 2026-04-29T19:29:32

D10 결정 — **default 표 자체 폐지 + 자동 마이그레이션 (B3 + M1)**.

사용자 통찰: "이 플러그인은 특정 언어를 대상으로 하진 않아" → default 5 언어 enumeration 자체가 Premise 2 ("v1 = 언어·프레임워크·도메인 구조 가정 제거") 와 모순. SKILL.md 본문에서 default 표 폐지 + README example block 으로 학습 가이드만 제공. backward-compat 은 doctor 의 자동 마이그레이션으로 보장.

## 변경 범위 (8 곳)

### A. SKILL.md 의 default 표 폐지 (B3)

1. **`pilot/skills/learn/SKILL.md`** — Phase 2 본문에서 의존성 추적 표 + 역할 분류 long-form 표 모두 **제거**. 표 위 Q1 blockquote 도 함께 제거 (적용 대상 없음). config 비어있을 때 폴더 인접성 fallback (이미 SKILL.md Phase 2 fallback 룰 존재) 동작 명시. **#02·#03 의 Q1 blockquote (analyze 5-2 + project H3 동적) 는 그대로 유지** — D10 영향은 learn 의 두 표만.

### B. config.md template 의 default 행 제거

2. **`workspace/context/config.md`** — `## learn 언어 패턴` 의 두 표 본문에서 **default 5 언어 행 모두 제거**. 헤더만 유지 (빈 표). 사용자가 자기 패턴 정의. (#02 의 `## scope 카테고리` 는 그대로 — D10 영향은 learn 만.)

### C. fixture pass-valid 갱신

3. **`pilot/tests/fixtures/v0.1.0-baseline/config/pass-valid/config.md`** — `## learn 언어 패턴` 의 두 표 행 모두 제거 (헤더만). PASS 케이스로 유지 — D10 결정으로 빈 표 정상.

### D. doctor 검증 룰 조정 (행 수 0 허용)

4. **`pilot/tools/doctor/integrity.py`** — `check_workspace_config_sections` 의 컬럼 수 검증 유지 (정확히 2 컬럼·헤더 정확 일치) + **행 수 0 허용** 명시. 빈 표는 ERROR 가 아닌 INFO ("default 폐지 — 폴더 인접성 fallback 동작").

5. **`pilot/tests/tools/test_doctor_integrity.py`** — 새 케이스 추가:
   - `test_pass_empty_table` — 헤더만 있고 행 0 → PASS (빈 표 허용)
   - 기존 `test_pass_valid` 갱신 — 빈 표 형태로 PASS 검증

### E. README example block 신설 (학습 가이드)

6. **`pilot/README.md`** 또는 **`pilot/skills/learn/README.md`** (신규) — v0.1.0 default 5 언어 의존성 추적 표 + 역할 분류 long-form 6 행 표 + scope 카테고리 3 행 표 example block 게시. 사용자가 복사하여 자기 `workspace/context/config.md` 에 붙여넣을 수 있게.

### F. M1 자동 마이그레이션 (features/05)

7. **`pilot/tools/doctor/integrity.py`** — `run_auto_fixes` 단계에 신규 함수 `migrate_v0_1_to_v0_2`:
   - 감지 조건: `.agent-state.yml.plugin_version` 이 `0.1.x` 또는 부재 + 현재 plugin v0.2.0 + `workspace/context/config.md` 의 `## learn 언어 패턴` 두 표 모두 빈 행.
   - 동작: 사용자 확인 (`a` 주입 / `b` 거부 / `c` 미루기) → `accepted` 시 v0.1.0 default 표 자동 주입 + `.agent-state.yml.migration_v0_2_0: accepted`. `declined` 시 빈 채로 + 기록. `migration_v0_2_0` 가 null 이면 다음 호출 시 다시 묻기.
   - `plugin_version` 갱신: 자동 주입/거부 결정 후 `0.2.0` 으로.

8. **`pilot/skills/doctor/SKILL.md`** — `--fix` 옵션 본문에 마이그레이션 동작 명시.

9. **`pilot/tests/tools/test_doctor_migration.py`** (신규) — 마이그레이션 단위 테스트:
   - PASS: v0.1.0 감지 + opt-in → 자동 주입 + .agent-state.yml 갱신
   - PASS: v0.1.0 감지 + opt-out → 빈 채로 + .agent-state.yml.migration_v0_2_0: declined
   - PASS: 신규 사용자 (`plugin_version: 0.2.0` 부터) → skip
   - PASS: 부분 정의 사용자 → skip + INFO

10. **`pilot/tests/fixtures/v0.1.0-baseline/migration/`** (신규) — pre/post-migration config 캡처.

## 적용 형식 (default 폐지 후 config.md 형태)

```markdown
## learn 언어 패턴

`/pilot:learn` 의 Phase 2 (Inventory) 가 진입 파일 확장자에서 언어를 추론한 뒤 본 섹션의 두 표를 lookup. 비어있으면 폴더 인접성 fallback. 사용자가 자기 프로젝트의 패턴을 정의.

### 의존성 추적

| 언어 | 의존성 추출 패턴 |
| ---- | ---------------- |

### 역할 분류

| 역할 | 식별 패턴 |
| --- | --------- |
```

## 핵심 원칙

- **pilot 은 특정 언어를 대상으로 하지 않는다** — default enumeration 자체가 이 명제와 모순.
- **사용자 자유 정의 우선** — config 가 1 급 시민, SKILL.md 는 메커니즘만.
- **backward-compat 은 마이그레이션으로 보장** — D1·D9 의 "default 표가 살아있을 때만 유효" 한 약속을 M1 으로 대체.

## 기존 D1·D9 결정 변경

- **D1 (wide-form 유지)** + **D9 (long-form 전환)** 모두 default 표가 SKILL.md 에 존재한다는 전제. D10 은 default 자체 폐지로 D1·D9 의 backward-compat 메커니즘을 마이그레이션으로 이동. **D9 의 long-form 형식은 유지** (config 의 역할 분류 표가 long-form 2 컬럼).

## design doc·features 갱신 완료 (메인 turn)

- `docs/design-pilot-generic-2026-04-29.md` — Premise 2 강화 + Reviewer Concerns D10 row.
- `features/01-learn-language-pattern-externalize.md` — D10 default 폐지 명시.
- `features/04-doctor-config-validation.md` — 컬럼 수 검증 + 행 수 0 허용.
- `features/05-config-default-migration.md` — 신설.
- `project.md` 목표 6 개 (#00·#01·#02·#03·#04·#05). #01·#04 는 D10 으로 [ ] 로 되돌림. #02·#03 는 [x] 유지 (D10 무관).
- `.agent-state.yml` `last_analyzed_features: 6`.

## prompts/ 갱신 보류

`prompts/{planner,evaluator}.md` 의 `[analyze-managed]` 영역은 본 메인 turn 에서 갱신 안 함. generator 작업 완료 후 evaluator 가 자체 책임으로 #01·#04·#05 체크리스트 갱신 + project.md 의 #01·#04·#05 [x] 처리. 또는 후속 `/pilot:analyze --regen-agents` 1 회로 일괄 정렬.

본 .focus.md 는 후속 generator 가 위 8~10 곳 변경을 일관 적용하도록 가이드.
